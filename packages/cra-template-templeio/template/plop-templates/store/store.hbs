import makeStore from '../makeStore';
import reducer from './reducer';
import { State } from './interface'

const initialState: State = undefined;

const [{{pascalCase name}}Provider, use{{pascalCase name}}Dispatch, use{{pascalCase name}}] = makeStore(
  reducer,
  initialState,
);

export { {{pascalCase name}}Provider, use{{pascalCase name}}Dispatch, use{{pascalCase name}} };
